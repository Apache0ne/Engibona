# Clean run required

The official architecture smoke and full test suite must pass on a clean runner before the new architecture-integration claims are treated as independently reproduced. The workflow is present in `.github/workflows/evidence-max.yml`.
