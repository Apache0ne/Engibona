# Experiment naming

Result files should use:

```text
<architecture>_<mode>_<method>_<seed_count>seeds_<commit>.json
```

Each JSON must include the full commit SHA, package versions, command line, architecture configuration, seeds, per-seed metrics, aggregate metrics, and wall time.
