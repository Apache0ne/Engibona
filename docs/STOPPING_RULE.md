# Evidence stopping rule

A component is considered settled for the public reconstruction only when it is format-compatible, wins controlled multi-seed tests, survives clean-run reproduction, and has no remaining untested higher-confidence alternative that matches the same deployment constraints.
