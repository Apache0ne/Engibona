# CI trigger note

A dedicated evidence branch and pull request should be used to inspect pull-request-triggered workflow runs and retrieve the official architecture JSON artifact through the connector.
